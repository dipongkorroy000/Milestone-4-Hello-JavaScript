let isLeader = true;
// if(isLeader = true) = if(isLeader)
// if(isLeader = false) = if(!isLeader)
// !true = false
// !false = true
if(!isLeader){
    console.log('leader coming');
}
else {
    console.log('out of the stage');
}